#include "main.h"
/**
 * _abs - returns absolute value of integer
 * @i: input integer
 * Return: Returna absolute value
 */
int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}
